# GULP-HANDLEBARS

###### A ready **_gulpfile_** preset for easiest start of the new project.

<hr>
After your clone run for development:

```
npm i && npm run dev
```

or for production:

```
npm i && npm run build
```
